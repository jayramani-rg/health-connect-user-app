// src/assets/images/index.ts
// Root asset barrel for feature-grouped images (Sec 3.1 folder tree).
// profileImages.ts is the legacy shim during migration — new code should
// prefer importing directly from this canonical registry once migration
// is complete.

export { default as images } from '../profileImages';
